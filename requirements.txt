# STRATA runtime dependencies. Pinned to major versions, all installable from a
# local wheelhouse in an air-gapped deployment. None of them phones home.
pydantic>=2.9,<3
PyYAML>=6.0
zstandard>=0.22
pyarrow>=17.0
fastapi>=0.115
uvicorn>=0.30
duckdb>=1.1
